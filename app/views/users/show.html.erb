<div class="container my-5">
  <div class="row">
    <div class="col-md-4">
      <div class="card">
        <div class="card-body">
          <h2 class="card-title"><%= @user.username %></h2>
          <p class="card-text">
            <strong>Name:</strong> <%= @user.first_name %> <%= @user.last_name %>
          </p>
          <p class="card-text">
            <strong>Email:</strong> <%= @user.email %>
          </p>
          <p class="card-text">
            <strong>Joined:</strong> <%= @user.created_at.strftime('%Y年%m月%d日') %>
          </p>
          
          <% if current_user == @user %>
            <%= link_to 'Edit Profile', edit_user_registration_path, class: 'btn btn-primary' %>
          <% end %>
        </div>
      </div>
    </div>
    
    <div class="col-md-8">
      <h3>User Activity</h3>
      <!-- ユーザーの投稿や活動をここに表示 -->
      <div class="alert alert-info">
        このユーザーはまだ投稿していません。
      </div>
    </div>
  </div>
</div>